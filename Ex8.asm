8) Crie uma calculadora que realize o cálculo de 2 números inteiros:

include emu8086.inc

org 100h
PRINT "DIGITE UM NUMERO: "

MOV AH, 1
INT 21H
SUB AL, 48
MOV BL, 100
MOV AH, 0
MUL BL
MOV PRIMEIRO, AX

MOV AH, 1
INT 21H
SUB AL, 48
MOV BL, 10d
MUL BL
MOV AH, 0d
ADD PRIMEIRO, AX 

MOV AH, 1
INT 21H
SUB AL, 48
MOV AH, 0
ADD PRIMEIRO, AX



PUTC 10
PUTC 13
PRINT "ESCOLHA A OPERACAO: (+ - / *)  "
MOV AH, 1
INT 21H
MOV DL, AL                       


PUTC 10
PUTC 13
PRINT "DIGITE OUTRO NUMERO: " 
MOV AH, 1
INT 21H
SUB AL, 48
MOV BL, 100
MOV AH, 0
MUL BL
MOV SEGUNDO, AX

MOV AH, 1
INT 21H
SUB AL, 48
MOV BL, 10d
MUL BL
MOV AH, 0d
ADD SEGUNDO, AX 

MOV AH, 1
INT 21H
SUB AL, 48
MOV AH, 0
ADD SEGUNDO, AX

CMP DL, 42d
JE MULT

CMP DL, 43d
JE SOMA

CMP DL, 45d
JE SUBT

CMP DL, 47d
JE DIVI

JMP exit
 
MULT:
 MOV AX, PRIMEIRO
 MUL SEGUNDO
 MOV RESPO, AX
 JMP ESC
 
SOMA:
 MOV AX, PRIMEIRO
 MOV DX, SEGUNDO
 ADD AX, DX
 MOV RESPO, AX 
 JMP ESC
 
 SUBT:
 
 MOV AX, PRIMEIRO
 MOV DX, SEGUNDO
 SUB AX, DX
 CMP AX, 0d
 JS NEGATIVO
 MOV RESPO, AX
 JMP ESC
 
 NEGATIVO:
 MOV S , AX
 MOV CX, AX
 SUB CX, AX
 SUB CX, AX
 MOV RESPO, CX
 JMP ESC
 
DIVI:
 
 MOV RESPO, 0
 MOV RESTO, 0
 MOV AX, PRIMEIRO
 MOV DX, 0
 DIV SEGUNDO   
 MOV RESTO, DX
 MOV RESPO, AX 
 JMP ESC2

ESC:
 
 PUTC 10
 PUTC 13
 PRINT "O RESULTADO EH: "
 
  
 CMP S, 0d         
 JS B
 JMP N
 B:
   PRINT "-"
        
 N:
 MOV AX, RESPO
 MOV BL, 100d
 DIV BL
 MOV DL, AL
 ADD DL, 48d
 MOV DH, AH
 MOV AH, 2d
 INT 21h
 
 MOV AH, 0d
 MOV AL, DH
 MOV BL, 10d
 DIV BL
 MOV DH, AH
 MOV DL, AL
 ADD DL, 48d
 MOV AH, 2d
 INT 21h
 
 MOV DL, DH 
 ADD DL, 48D
 INT 21h
  
 JMP EXIT
  
ESC2:
 
 PUTC 10
 PUTC 13
 PRINT "O RESULTADO EH: "
 
 MOV AX, RESPO
 MOV BL, 100d
 DIV BL
 MOV DL, AL
 ADD DL, 48d
 MOV DH, AH
 MOV AH, 2d
 INT 21h
 
 MOV AH, 0d
 MOV AL, DH
 MOV BL, 10d
 DIV BL
 MOV DH, AH
 MOV DL, AL
 ADD DL, 48d
 MOV AH, 2d
 INT 21h
 
 MOV DL, DH 
 ADD DL, 48D
 INT 21h
 
 PUTC 10
 PUTC 13
 PRINT "O RESTO EH: "
 MOV AX, RESTO
 MOV BL, 100d
 DIV BL
 MOV DL, AL
 ADD DL, 48d
 MOV DH, AH
 MOV AH, 2d
 INT 21h
 
 MOV AH, 0d
 MOV AL, DH
 MOV BL, 10d
 DIV BL
 MOV DH, AH
 MOV DL, AL
 ADD DL, 48d
 MOV AH, 2d
 INT 21h
 
 MOV DL, DH 
 ADD DL, 48D
 INT 21h 

RET
EXIT:

PRIMEIRO DW 1H 
SINAL    DW 1h
SEGUNDO  DW 1h
RESPO    DW 1h
RESTO    DW 1h
S        DW 1h


